document.addEventListener('DOMContentLoaded', () => {
    // Note: Remove buttons (X) are submit buttons in the form and work naturally
    // They don't need JavaScript handling - they submit the form which removes the item
    
    // Quantity controls are handled by cart-quantity.js
    // This file is kept for backward compatibility but most functionality is in cart-quantity.js
});
